{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb", // 1
            "request": "launch",
            "name": "Launch Plywood (debug)",
            "program": "${workspaceFolder}/.build/debug/Plywood", // 2
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "swift-build" // 3
        }
    ]
}